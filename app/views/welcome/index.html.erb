<% if @rippples.present? %>
  <% @rippples.each do |r| %>
    <div class='date'><%= r.created_at.strftime('%b %d, %Y') %></div>
    <div class='title'><%= r.title %></div>
  <% end %>
<% else %>
<div class='wrapper'>
  <p class='center'>Sorry, there don't seem to be any rippples showing up yet.</p>
</div>
<%= @content %>
<% end %>
